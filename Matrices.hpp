class Matrices{
	
	
	public:
	   	Matrices(int ,int );
	   	float ingresarM();
	   	float Mostrar();
	   	float suma(Matrices ,Matrices );
		float getNumero(int ,int );
		float MultiEs(float );
	private:
	  	int fila;
	  	int columna;
	  	float Matriz[100][100];
		
};
